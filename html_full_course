alert() = displays the input in the top screen of the page
prompt() = gets input from user in the top screen of the page
Math.floor(num) = Round down
Math.ceil(num) =Round up
Math.random() = 0 inclusive 1 exclusive

To insert a argument in the string within a function => `${  }`
map function = .map()
filter function 
Reduce function - usually for summing up

